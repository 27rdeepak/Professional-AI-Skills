interface:
  display_name: "First Principles Analysis"
  short_description: "Decompose ambiguous problems into constraints, facts, assumpt..."
  default_prompt: "Use $first-principles-analysis to separate real constraints from conventions, test the key assumptions, and rebuild the options."
