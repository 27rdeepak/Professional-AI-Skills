# Architecture

Skills define behavior and output contracts. Frameworks provide shared methods; templates provide output shapes; playbooks coordinate stages; personas add stakeholder lenses without invented authority; evaluations test observable behavior; platform packs explain loading only.

Dependency flows from platform packs and playbooks toward skills, then optional frameworks and resources. Core skill behavior never depends on a platform pack.
